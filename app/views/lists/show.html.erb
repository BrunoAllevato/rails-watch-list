<div class="container mt-3">
  <div class="row my-2">
    <h1 class="fw-bold opacity-75"><%= @list.name %></h1>

    <%= link_to list_path(@list), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} do %>
      <i class="fa-solid fa-trash opacity-75 link-secondary"><small class="">Delete List</small></i>
    <% end %>
  </div>

  <div class="mt-3">
    <%= simple_form_for [@list, @bookmark] do |f| %>
      <div class="modal-body">
        <%= f.association :movie, collection: Movie.all.order(:title), prompt: "Select a movie" %>
        <%= f.input :comment, placeholder: "Enter a comment about the movie" %>
      </div>
      <div class="modal-footer">
        <%= f.submit "Add movie", class: "btn btn-flat" %>
      </div>
    <% end %>
  </div>

  <hr class="mt-2">

  <div class="mt-4">
    <div class="row row-cols-1 row-cols-md-4 g-4 d-flex justify-content-around">
      <% @list.movies.each do |movie| %>
        <div class="col-4" style="width: 350px">
          <div class="card h-100">
            <img src="<%= movie.poster_url %>" class="card-img-top" height="300px" alt="...">
            <div class="card-body">
              <strong class="card-title"><%= movie.title %> <span class="text-muted font-weight-normal">• <%= movie.rating %> <i class="fas fa-star star-yellow"></i></span></strong>
              <p class="card-text"><small class="text-muted"><%= movie.overview %></small></p>
              <% bookmark = Bookmark.find_by(list: @list, movie: movie) %>
              <hr>
              <p class="card-text">
                <div class="d-flex align-items-end justify-content-between pt-0">
                  <div>
                    <i class="fas fa-quote-left me-2"></i><small class="flex-grow-1"><%= bookmark.comment %></small>
                  </div>
                  <small class="text-right flex-shrink-0"><%= link_to "delete", bookmark_path(bookmark), data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to remove #{movie.title} from your #{@list.name} list"}, class: 'text-danger' %></small>
                </div>
              </p>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <div class="row mb-3 mt-4">
    <div class="col-2">
      <%= link_to "Go to Bookmarks", new_list_bookmark_path(@list), class: "text-decoration-none" %>
      <br>
      <%= link_to "Back to Homepage", lists_path, class: "text-decoration-none" %>
    </div>
  </div>

</div>
